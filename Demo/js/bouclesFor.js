// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }

for (let index = 10; index >= 1; index--) {
    console.log(index);       
}
console.log("Décollage!");

const tabSemaine = [
    "lundi",
    "mardi",
    "mercredi",
    "jeudi",
    "vendredi",
    "samedi",
    "dimanche",
]

for (index = 0 ; index < tabSemaine.length ; index++) {
    console.log(tabSemaine[index]); 
}

